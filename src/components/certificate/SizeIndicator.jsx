import React from 'react';

const SizeIndicator = () => {
  return (
    <div className="text-sm text-gray-500 mt-2 bg-gray-100 px-3 py-1 rounded-md shadow-sm inline-block">
      A4 Landscape (297mm × 210mm)
    </div>
  );
};

export default SizeIndicator;
